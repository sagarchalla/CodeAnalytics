package chapter5.trees.trees.trees;

public class TreeIsBalannced {

    // check if the tree is height balanced

    // main method to to create a tree
    public static void main(String[] args) {

       /* TreeNode root = new TreeNode(5);
        root.left = new TreeNode(6);
        root.right = new TreeNode(8);
        root.left.left = new TreeNode(10);
        root.left.right = new TreeNode(11);
        root.left.left.right = new TreeNode(13);
        root.left.left.left = new TreeNode(4);*/
    }

    // method to check if tree is balanced
    // a tree is balanced if theskt

    //1st Approach



}
